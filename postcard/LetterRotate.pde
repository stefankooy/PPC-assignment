class LetterRotate extends Letter {
  
  //int angle, rotateSpeed, xpos;

  LetterRotate(char c) {
    super(c);
    //this.rotateSpeed = rotateSpeed;
    //this.angle = 2;
  }

  //void update() {
  //  angle += rotateSpeed;
  //}
  
  void display() {
    //pushMatrix();
    //translate(xpos, ypos);
    //rotate(angle);
    //text(c2, 0, 0);
    //popMatrix();

    //fill(textColor);
    //textSize(textSize);

  }
}
